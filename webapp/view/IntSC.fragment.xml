<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core">
	<SelectDialog
		noDataText="No hi ha integracions actives"
		title="Selecciona integració"
		confirm="handleIntegra"
		items="{SCToIntAct}">
		<StandardListItem
			title="{DescrInt}"
			description="{DescrSub}"
			info="{Interfase} {InterfaseSub}"
			type="Active"
			iconDensityAware="{Documents}"/>
	</SelectDialog>
</core:FragmentDefinition>